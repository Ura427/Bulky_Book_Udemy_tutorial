@model IEnumerable<CoverType>

@{
    ViewData["Title"] = "Index CoverType";
}


<div class="containder p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">CoverType List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
                <i class="bi bi-plus-circle"></i> &nbsp; Create new CoverType
            </a>
        </div>
    </div>


    <table class="table table-bordered table-striped" style="width:100%">
        <thead>
            <tr>
                <th>CoverType name</th>
                <th>Display order</th>
                <th></th>
                <th></th>
            </tr>
        </thead>
        <tbody>

            @foreach (var coverType in Model)
            {
                <tr>
                    <td width="50%">@coverType.Name</td>
                    <td>
                        <div class="w-auto btn-group" role="group">
                            <a asp-controller="CoverType" asp-action="Edit" asp-route-id="@coverType.Id" class="btn btn-info"><i class="bi bi-pencil-square"></i>Edit</a>
                        </div>
                    </td>
                    <td>
                        <div class="w-auto btn-group" role="group">
                            <a asp-controller="CoverType" asp-action="Delete" asp-route-id="@coverType.Id" class="btn btn-warning"><i class="bi bi-trash-fill"></i>Delete CoverType</a>
                        </div>
                    </td>
                </tr>
            }

        </tbody>
    </table>

</div>